/*JavaScript Identifier Rules
1. Allowed characters: Letters (a-z, A-Z), digits (0-9), underscore (_), and dollar sign ($)
2. First character: Must be a letter, underscore, or dollar sign; cannot be a digit
3. Case-sensitive: name and Name are different
4. No spaces or special symbols (except _ and $)
5. Cannot be a reserved keyword (e.g., var, let, const, function, class, return)
Valid Identifiers
- userName
- _count
- $button
- MAX_SIZE
- data1
- camelCaseExample
Invalid Identifiers
- 1stUser (starts with digit)
- user-name (contains hyphen)
- user name (contains space)
- var (reserved keyword)
- user@id (contains special character) */

var $ = 10;
var _a = 12;
var b = 20;
var abc123 = 30;
var Name = "Preeti";
var name = "P3"
